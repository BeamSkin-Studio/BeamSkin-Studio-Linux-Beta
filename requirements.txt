# Install with: pip install -r requirements.txt

# ── Core GUI Framework (PySide6 edition) ──────────────────────────────────────
PySide6>=6.5.0

# ── Image Processing ──────────────────────────────────────────────────────────
Pillow>=10.0.0
imageio>=2.28.0

# ── HTTP Requests (update checker, online tab) ────────────────────────────────
requests>=2.31.0

# ── Windows-specific (optional — better taskbar / file-dialog integration) ────
pywin32>=306; sys_platform == 'win32'

# ── Note for Linux users ──────────────────────────────────────────────────────
# PySide6 bundles its own Qt libraries, so no system Qt packages are needed.
# You may still need libGL for rendering:
#   Ubuntu/Debian: sudo apt install libgl1
#   Fedora:        sudo dnf install mesa-libGL
#   Arch:          sudo pacman -S mesa
